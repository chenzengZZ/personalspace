# 当前任务状态

## 2026-03-21 并行线程补记：Builder 手册学习版网页表现层重构

### 当前线程目标

- 在不改 `builder-manual/AI-BUILDER-MANUAL-v1.1.md` 正文内容的前提下
- 重构 `builder-manual/LEARNING_BOOK.html` 的表现层、交互层与渲染层
- 优先把阅读体验做成“可本地直接打开”的高质感网页书

### 当前范围

只做：

- 页面布局与视觉系统升级
- 目录、进度、跳转、阅读定位等交互升级
- CSS / JS 架构重整
- 本地打开可用前提下的轻量动效与响应式优化

不做：

- 改章节顺序
- 改标题文字
- 改段落文字
- 改列表内容
- 引入复杂构建流程

### 当前判断

- 这轮不拆多 agent
- 原因不是不能拆，而是主战场集中在同一组网页文件
- 若并行编辑同一页面壳层，集成成本和风格失真风险高于提速收益

### 验收标准

- 正文内容保持不变
- 本地双击 HTML 可正常打开
- 桌面与移动端可读性明显提升
- 页面气质、动效与导航体验明显强于当前版本

## 当前主目标

送水小程序以“稳定上线、长期运维”为目标推进。

## 当前阶段

- C 端本地最小闭环已基本完成
- 当前进入“下一任务裁决 / 人工回归 / 云前对齐准备”阶段

## 当前任务

### 任务名称

下一轮任务裁决

### 地图归类

- 主层：任务层
- 次层：交付层（人工回归、排查、最小可观测）
- 次层：AI 编排层（是否开新线程、是否切窗口、是否需要多 agent）

### 为什么现在推进这个任务

- 最近一轮本地功能实现已较多
- 当前最需要的是收口、验收、排序，而不是继续堆功能
- 云端接入仍属于高冲突主题，进入前要先把本地与文档对齐

## 当前候选任务

### 候选 1：开发者工具人工回归

- 手动验证地址管理、确认订单、订单搜索、空状态
- 确认页面交互和文案是否顺手

### 候选 2：云端地址 / 订单契约对齐

- 检查本地数据结构与云函数输入输出是否一致
- 为下一轮云端接入做准备

### 候选 3：最小可观测性补强设计

- 明确关键业务节点应该记录什么
- 区分用户提示、开发排查信息、最小日志需求

## 本轮范围

只做：

- 明确下一轮最值得推进的一个任务
- 确认依赖、风险和验收标准
- 保持项目文档与当前认知同步

不做：

- 直接进入新的高风险功能开发
- 直接改造支付 / 云端 / 核心状态流转
- 同时推进多个高冲突主题

## 当前决定

- 默认先收口、再扩展
- 默认先局部诊断、再全局扫描
- 默认先设计和留档，再进入高风险实现

## 依赖与风险

### 风险等级

- 中风险

### 风险说明

- 如果继续在未回归、未留档的状态下推进，容易把问题叠加起来
- 云端接入前若本地契约未明确，后续联调成本会上升

## 验收标准

- 明确下一轮只做什么
- 明确下一轮不做什么
- 明确是否依赖云端 / 外部条件
- 明确是否需要新线程或新窗口

## 下一轮建议

优先顺序建议：

1. 开发者工具人工回归
2. 云端契约对齐设计
3. 最小可观测性补强
